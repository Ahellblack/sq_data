server:
  port: 8066
  context-path: /sq_data

quartz:
  enabled: true
spring:
  thymeleaf:
    mode: LEGACYHTML5
  profiles:
     active: test
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true

version:
  pcversion: "V1.1" # 版本号
  build: 1111 # 小版本号
